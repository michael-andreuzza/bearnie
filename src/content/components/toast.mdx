---
title: Toast
description: Temporary notification messages.
tagline: Notification messages
category: feedback
status: stable
order: 20
---

import ComponentPreview from "@/components/docs/ComponentPreview.astro";
import CodeBlock from "@/components/docs/CodeBlock.astro";
import Prose from "@/components/docs/Prose.astro";
import Button from "@/components/ui/button/Button.astro";
import Toaster from "@/components/ui/toast/Toaster.astro";

<Toaster />

<ComponentPreview>
  <Fragment slot="code">
```html
<Button onclick="toast('Event has been created.')">
  Show Toast
</Button>
```
  </Fragment>
  <Button onclick="toast('Event has been created.')">Show Toast</Button>
</ComponentPreview>

<div class="space-y-4">
<Prose>
### Installation
</Prose>

<ComponentPreview component="toast" />
</div>

<div class="space-y-4">
<Prose>
### Usage

```ts
import { Toaster } from "@/components/bearnie/toast"
```
```js
toast("Event has been created.")
```

</Prose>
</div>

<div class="space-y-8">
<Prose>
### Examples
</Prose>

<div class="space-y-4">
<Prose>
#### With title and description
</Prose>

<ComponentPreview>
  <Fragment slot="code">
```html
<Button onclick="toast({ title: 'Scheduled', description: 'Friday, February 10, 2023 at 5:57 PM' })">
  Show Toast
</Button>
```
  </Fragment>
  <Button onclick="toast({ title: 'Scheduled', description: 'Friday, February 10, 2023 at 5:57 PM' })">
    Show Toast with details
  </Button>
</ComponentPreview>
</div>

<div class="space-y-4">
<Prose>
#### Success
</Prose>

<ComponentPreview>
  <Fragment slot="code">
```html
<Button onclick="toast.success('Changes saved successfully!')">
  Show Success Toast
</Button>
```
  </Fragment>
  <Button variant="success" onclick="toast.success('Changes saved successfully!')">
    Show Success Toast
  </Button>
</ComponentPreview>
</div>

<div class="space-y-4">
<Prose>
#### Error
</Prose>

<ComponentPreview>
  <Fragment slot="code">
```html
<Button onclick="toast.destructive('Something went wrong.')">
  Show Error Toast
</Button>
```
  </Fragment>
  <Button variant="destructive" onclick="toast.destructive('Something went wrong.')">
    Show Error Toast
  </Button>
</ComponentPreview>
</div>

<div class="space-y-4">
<Prose>
#### Warning
</Prose>

<ComponentPreview>
  <Fragment slot="code">
```html
<Button onclick="toast.warning('Your session is about to expire.')">
  Show Warning Toast
</Button>
```
  </Fragment>
  <Button variant="warning" onclick="toast.warning('Your session is about to expire.')">
    Show Warning Toast
  </Button>
</ComponentPreview>
</div>

<div class="space-y-4">
<Prose>
#### Info
</Prose>

<ComponentPreview>
  <Fragment slot="code">
```html
<Button onclick="toast.info('A new update is available.')">
  Show Info Toast
</Button>
```
  </Fragment>
  <Button variant="info" onclick="toast.info('A new update is available.')">
    Show Info Toast
  </Button>
</ComponentPreview>
</div>

<div class="space-y-4">
<Prose>
#### Promise

Shows a loading toast that turns into a success or error toast when the promise settles.
</Prose>

<ComponentPreview>
  <Fragment slot="code">
```js
toast.promise(saveSettings(), {
  loading: "Saving…",
  success: "Settings saved.",
  error: (err) => `Could not save: ${err.message}`,
})
```
  </Fragment>
  <Button variant="outline" onclick="toast.promise(new Promise((resolve) => setTimeout(resolve, 2000)), { loading: 'Saving…', success: 'Settings saved.', error: 'Could not save.' })">
    Show Promise Toast
  </Button>
</ComponentPreview>
</div>

<div class="space-y-4">
<Prose>
#### With action
</Prose>

<ComponentPreview>
  <Fragment slot="code">
```html
<Button onclick="toast({ title: 'Undo action', description: 'Message deleted.', action: { label: 'Undo', onClick: () => console.log('Undone!') } })">
  Show Toast with Action
</Button>
```
  </Fragment>
  <Button variant="outline" onclick="toast({ title: 'Undo action', description: 'Message deleted.', action: { label: 'Undo', onClick: () => console.log('Undone!') } })">
    Show Toast with Action
  </Button>
</ComponentPreview>
</div>
</div>

<Prose>
### Anatomy

- `Toaster` - The container that renders toasts (add once to layout)
</Prose>

<Prose>
### Props

#### Toaster

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| position | top-left \| top-center \| top-right \| bottom-left \| bottom-center \| bottom-right | bottom-right | Toast position |
| max | number | 5 | Maximum toasts shown at once; the oldest is dismissed when exceeded |
| class | string | - | Additional CSS classes |

#### toast() function

| Option | Type | Default | Description |
|--------|------|---------|-------------|
| title | string | - | Toast heading |
| description | string | - | Toast message |
| type | default \| success \| destructive \| warning \| info | default | Toast variant |
| duration | number | 4000 | Auto-dismiss time (ms). Hovering a toast pauses the timer |
| action | object | - | Action button config |

Helpers: `toast.success()`, `toast.destructive()`, `toast.warning()`, `toast.info()`, `toast.promise(promise, { loading, success, error })`, and `toast.dismiss(el)`. The `success` and `error` messages accept a string, an options object, or a function receiving the resolved value / rejection reason.
</Prose>
